services:
  vw-bot:
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      TZ: Asia/Tokyo
    env_file:
      - ${ENV_FILE:-.env.dev}
    container_name: vw-bot
    restart: always
